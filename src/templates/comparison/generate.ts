import type { ComparisonData } from "./schema";

// Stub: real implementation lands in Step 9.
export async function generate(_data: ComparisonData): Promise<Blob> {
  throw new Error("Comparison 下載尚未實作");
}
